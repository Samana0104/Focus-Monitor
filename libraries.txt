# Add one pip package per line.
# Version constraints are supported, for example:
# numpy==2.0.1
# opencv-python>=4.10

numpy
pandas
opencv-python
mediapipe
pyside6
insightface
ultralytics
pytest
# Torch and ONNX Runtime are selected by library_installer.bat for GPU or CPU.
